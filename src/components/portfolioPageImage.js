import React from "react"

export default function PortfolioPageImage({ children }) {
    return (
        <div style={{ maxWidth: "800px", textAlign: "center", margin: "0 auto" }}>
            {children}
        </div>
    )
}